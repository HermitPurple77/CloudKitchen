# .github/workflows/ci.yml

name: Kitchen Management CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ── 1. Unit Tests ────────────────────────────────────────────────────────────
  unit-tests:
    name: Unit Tests (Jest)
    runs-on: ubuntu-latest

    services:
      mongo:
        image: mongo:6
        ports:
          - 27017:27017

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install backend dependencies
        run: cd backend && npm install

      - name: Run unit tests
        run: cd backend && npm test
        env:
          MONGO_URI: mongodb://localhost:27017/kitchendb_test

  # ── 2. Build Docker Images ───────────────────────────────────────────────────
  build-images:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: unit-tests

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Backend Image
        run: docker build -t kitchen-backend ./backend

      - name: Build Frontend Image
        run: docker build -t kitchen-frontend ./frontend

      - name: Build Selenium Image
        run: docker build -t kitchen-selenium ./selenium

  # ── 3. Selenium UI Tests ─────────────────────────────────────────────────────
  selenium-tests:
    name: Selenium UI Tests
    runs-on: ubuntu-latest
    needs: build-images

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Start full stack
        run: |
          docker compose up -d mongo backend seeder frontend
          sleep 20

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python dependencies
        run: pip install selenium pytest pytest-html

      - name: Install Chrome & ChromeDriver
        uses: browser-actions/setup-chrome@v1

      - name: Run Selenium tests
        run: |
          cd selenium
          pytest tests/ -v --html=report.html --self-contained-html
        continue-on-error: true

      - name: Upload Selenium report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: selenium-report
          path: selenium/report.html

      - name: Tear down
        if: always()
        run: docker compose down

  # ── 4. Deploy Frontend → GitHub Pages ───────────────────────────────────────
  # Needs Pages enabled: repo Settings → Pages → Source → GitHub Actions
  deploy:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: selenium-tests
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    # Pages permissions must be on the JOB, not at workflow level
    permissions:
      contents: read
      pages: write
      id-token: write

    # Prevent two deploys running at the same time
    concurrency:
      group: pages
      cancel-in-progress: true

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # Inject the live backend URL into config.js before deploying.
      # Add BACKEND_URL as a GitHub Secret if you have a hosted backend.
      # If not set, pages will fall back to localhost (fine for local Docker demo).
      - name: Inject backend URL into config.js
        run: |
          BACKEND="${{ secrets.BACKEND_URL }}"
          if [ -z "$BACKEND" ]; then
            echo "ℹ️  No BACKEND_URL secret — keeping localhost default"
          else
            echo "🔧 Setting backend to: $BACKEND"
            sed -i "s|http://localhost:5000|${BACKEND}|g" frontend/config.js
          fi

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload frontend to Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./frontend

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Print live URL
        run: |
          echo "🚀 Live at: ${{ steps.deployment.outputs.page_url }}"
