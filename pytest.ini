[pytest]
minversion = 7.0
testpaths = tests
addopts =
    -ra
    --strict-markers
    --tb=short

markers =
    selenium: marks tests as Selenium UI tests (deselect with '-m "not selenium"')
    slow: marks tests as slow-running
    integration: marks integration tests

log_cli = true
log_cli_level = INFO
